
# Bit UI

Bit UI adalah koleksi komponen React yang dapat digunakan kembali untuk membangun aplikasi web dengan cepat dan mudah.

## 🚀 Instalasi

Untuk menginstal Bit UI, gunakan perintah berikut:

```bash
npm install bit-ui
```

## 📚 Komponen

Berikut adalah daftar komponen yang tersedia di Bit UI:

- Accordion
- Alert Dialog
- Alert
- Aspect Ratio
- Avatar
- Badge
- Breadcrumb
- Button Group
- Button
- Calendar
- Card
- Carousel
- Chart
- Checkbox
- Collapsible
- Combobox
- Command
- Context Menu
- Data Table
- Date Picker
- Dialog
- Drawer
- Dropdown Menu
- Empty
- Field
- Form
- Hover Card
- Input Group
- Input OTP
- Input
- Item
- Kbd
- Label
- Menubar
- Native Select
- Navigation Menu
- Pagination
- Popover
- Progress
- Radio Group
- Resizable
- Scroll Area
- Select
- Separator
- Sheet
- Sidebar
- Skeleton
- Slider
- Sonner
- Spinner
- Switch
- Table
- Tabs
- Textarea
- Toast
- Toaster
- Toggle Group
- Toggle
- Tooltip
- Typography

## 🎣 Hooks

Bit UI juga menyediakan berbagai hooks untuk mempermudah pengembangan aplikasi Anda:

- `useBattery` – monitor status baterai perangkat
- `useClickAway` – deteksi klik di luar elemen
- `useContinuousRetry` – retry otomatis sampai berhasil
- `useCopyToClipboard` – copy teks ke clipboard
- `useCountdown` – buat countdown timer
- `useCounter` – custom counter logic
- `useDebounce` – debouncing state/event
- `useDefault` – state dengan default value
- `useDocumentTitle` – ubah title dokumen
- `useEventListener` – pasang event listener
- `useFavicon` – ubah favicon dinamis
- `useFetch` – fetch data (loading/error/data)
- `useGeolocation` – akses posisi GPS
- `useHistoryState` – undo/redo state
- `useHover` – track hover state
- `useIdle` – detect user idle
- `useIntersectionObserver` – track visibility & intersection
- `useInterval` – interval loop biasa
- `useIntervalWhen` – interval dengan start/pause
- `useIsClient` – check apakah di client
- `useIsFirstRender` – cek render pertama
- `useKeyPress` – detect key press
- `useList` – manage & manip list
- `useLocalStorage` – sync state ke localStorage
- `useLockBodyScroll` – disable scroll
- `useLogger` – log lifecycle info
- `useLongPress` – long-press interactions
- `useMap` – sync state ke Map structure
- `useMeasure` – ukur dimensi element
- `useMediaQuery` – track media query
- `useMouse` – mouse position
- `useNetworkState` – network status
- `useObjectState` – object-based state
- `useOrientation` – device orientation
- `usePageLeave` – detect user leaving page
- `usePreferredLanguage` – browser language
- `usePrevious` – get previous value
- `useQueue` – manage queue
- `useRandomInterval` – random interval
- `useRenderCount` – count renders
- `useRenderInfo` – info tentang render
- `useScript` – load external script
- `useSessionStorage` – sync state ke sessionStorage
- `useSet` – sync state ke Set structure
- `useThrottle` – throttle updates
- `useTimeout` – timeout utilities
- `useToggle` – toggle boolean
- `useVisibilityChange` – document visibility
- `useWindowScroll` – scroll position
- `useWindowSize` – window size

## 📦 Publishing to npm

Untuk mempublikasikan versi baru ke npm, ikuti langkah-langkah berikut:

1.  **Perbarui versi patch:**

    ```bash
    npm version patch
    ```

2.  **Publikasikan ke npm:**

    ```bash
    npm publish
    ```

## 🤝 Kontribusi

Kontribusi selalu diterima! Silakan buka isu atau pull request untuk berkontribusi.

## 📄 Lisensi

Proyek ini dilisensikan di bawah [Lisensi MIT](LICENSE).
